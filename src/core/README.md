# Core

Framework-free model, IO, simulation, math, and constraint logic lives here.
Browser worker orchestration belongs in `platform`; lint rules keep UI, shell,
renderer, state-store, and browser-global dependencies out of core.
